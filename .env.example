# Database Configuration
# Option 1: Use DATABASE_URL (for Render/production)
# DATABASE_URL=mysql://user:password@host:port/dbname

# Option 2: Use individual DB_* variables (for local dev)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=finalneta

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# JWT Secret (change in production!)
JWT_SECRET=your-secret-key-change-in-production

# Email Configuration
EMAIL_MODE=shadow
EMAIL_SHADOW_ADDRESS=aineta502@gmail.com
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@aineta.in
SENDGRID_FROM_NAME=AI Neta

# Admin Token (for admin endpoints)
ADMIN_TOKEN=pilot-admin-qa

# Escalation Testing (optional)
TEST_ESCALATION_OVERRIDE_MINUTES=1
ESCALATION_WORKER_INTERVAL_SECONDS=30

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:3000

# Uploads Base Path
UPLOAD_BASE_PATH=uploads
